from agents.base import BaseAgent

class EmotionAgent(BaseAgent):
    name = "emotion"
    def execute(self, task):
        return "Bağlama uygun iletişim tonu belirlenecek; bu gerçek bilinç/duygu iddiası değildir."
